version: 2.1
jobs:
  build:
    docker:
      - image: alpine
    working_directory: ~/default-values-spike
    steps:
      - checkout
      - run:
          name: Run tests
          command: |
            echo 'In the command...'
            ls -a
            bash ./pp-default-values-alerting.sh